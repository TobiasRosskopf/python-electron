{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Electron",
            "type": "electron",
            "request": "launch",
            "appDir": "${workspaceFolder}",
            "sourceMaps": true
        },
    ]
}
